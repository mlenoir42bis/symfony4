{% extends 'base.html.twig' %}

    {% block body %}
        
<div class="section no-pad-bot" id="index-banner">
    <div class="container">
      <br><br>
      <h1 class="header center orange-text">ShowById</h1>
      <div class="row center">

        <h3>Title : </h3>
        <p>{{ book.title }}</p>
        <h3>content : </h3>
        <p>{{ book.content }}</p>

        {% if book.image.name is defined %}
          <img src="{{ asset('image/')~book.image.name }}">
        {% else %}
          <p>Pas d'image</p>
        {% endif %}

      </div>
      <br><br>

    </div>
  </div>
</div>



    {% endblock %}

