vich_uploader:
    db_driver: orm

    #mappings:
    #    products:
    #        uri_prefix: /images/products
    #        upload_destination: '%kernel.project_dir%/public/images/products'
    mappings:
        assemble_files:
            uri_prefix: /assemble
            upload_destination: '%kernel.project_dir%/public/assemble'