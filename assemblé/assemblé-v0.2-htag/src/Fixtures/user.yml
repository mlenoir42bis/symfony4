App\Entity\User:
  admin:
    email: admin@admin.fr
    roles: ['ROLE_ADMIN']
    password: '<encodePassword(admin)>'
  
  user:
    email: user@user.fr
    roles: ['ROLE_USER']
    password: '<encodePassword(user)>'